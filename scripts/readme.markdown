## To proper work we need to add the env variables at the runner:
```
PUPPETEER_BROWSER_REVISION=latest;
PUPPETEER_EXECUTABLE_PATH=C:\Program Files (x86)\Google\Chrome\Application\chrome.exe
```
### Also run the actual version of the node js and chrome.  